services:
  postgres:
    image: postgres:16-alpine
    container_name: catchmind-postgres
    environment:
      POSTGRES_USER: catchmind
      POSTGRES_PASSWORD: catchmind
      POSTGRES_DB: catchmind
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: catchmind-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  postgres-data:
  redis-data:
